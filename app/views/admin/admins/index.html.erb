<div>
  <h1  class="table-text-admins"> All admins</h1>
  <hr>
  <table id="example" class="table table-striped table-bordered table-admins">
    <thead>
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.first_name %></td>
        <td><%= admin.last_name %></td>
        <td><%= admin.email %></td>
        <td><%= link_to 'Show', [:admin, admin], class: 'btn btn-primary'%>
          <%= link_to 'Edit', [:edit, :admin, admin], class: 'btn btn-success' %>
          <%= link_to 'Destroy',  [:admin, admin], method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to 'New Admin ', new_admin_admin_path, class: 'table-button-admins btn btn-primary' %>
</div>