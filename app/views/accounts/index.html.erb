<div class="container-fluid">
  <h1 class="table-text-customers"> All Account</h1>
  <hr>
  <table id="example" class="table table-striped table-bordered">
    <thead>
      <tr>
        <!-- <th>customer_id</th>
        <th>accountant_id</th> -->
        <th>Firm Name</th>
        <th>Firm type</th>
        <th>no_of_employees</th>
        <th>Show</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
    </thead>
    <% if @accounts.present? %>
      <tbody>
        <%# @customer_accountants.each do |account| %>
          <!-- <tr>
           <td><%#= account.customer_id %></td>
           <td><%#= account.accountant_id %></td>
        <%# end %> -->
        <% @accounts.each do |account| %>
            <td><%= account.firm_name %></td>
            <td><%= account.firm_type %></td>
            <td><%= account.no_of_employees %></td>
            <td><%= link_to 'Show', account, class: 'btn btn-primary'%></td>
            <!-- <td><%#= link_to 'Edit',  edit_account_path(account), class: 'btn btn-success' %></td>
            <td><%#= link_to 'Destroy',  accounts_url, method: :delete, data: { confirm: 'Are you sure?'}, class: 'btn btn-danger' %></td> -->
          </tr>
        <% end %>
      </tbody>
    <% end %>
  </table>
  <%= link_to 'New Account ', new_account_path, class: 'table-button-customers btn btn-primary' %>
</div>